<div class="center hero-unit">
  <h1>Zim</h1>
  <p>
    An online IM.
  </p>
  <% if !signed_in? %>
  <%= link_to 'Sign Up', signup_path %>
  <% end %>
</div>
<% if signed_in? %>
<%= render 'contacts/contacts' %>
<% end %>
