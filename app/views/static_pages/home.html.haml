%div{ :class => "center hero-unit" }
  %h1 Zim
  %p An online IM.
  - if !signed_in?
    = link_to 'Sign Up', signup_path
  - if signed_in?
    %div{ :id => 'contacts-div' }
    :javascript
      $(function() {
        $("#contacts-div").load("#{contacts_path}");
      });
