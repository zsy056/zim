= render 'layouts/stylesheets'
= javascript_include_tag 'application'
= csrf_meta_tag
= will_paginate
%ul{ :class => 'messages' }
  - if @messages.size == 0
    No Record
  - @messages.each do |message|
    = render message
= will_paginate
%div{ :id => 'dialogs' }
